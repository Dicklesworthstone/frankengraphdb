# =============================================================================
# command_type_classification.toml — input-looking type classification (G0)
# =============================================================================
# Bead: fgdb-5uw2
# Provenance: plan §5.1 (COMPREHENSIVE_PLAN_FOR_THE_DESIGN_OF_FRANKENGRAPHDB.md
# line 294), verbatim: "The paired `command_type_classification.toml` assigns
# every independently addressable normative input-looking type—including every
# type whose stable name ends in `Spec`—exactly one class:
# `RegisteredCommandInput{command_contract_id}`, `PreOrderArtifact`,
# `CertificateInput`, `ResultOnly`, `ProjectionInput`, or `RuntimeOnly`.
# A suffix conveys no semantics."
#
# WHY THE CREATION POPULATION WAS THREE ROWS, NOT ~40+. MEASURED 2026-07-28 at
# HEAD 5d42e28: registries/appendix_a_catalog.toml carries 39 logical kinds
# whose stable name ends in Spec, plus further Spec-named top-level candidates.
# Nearly all of them are semantic or maintenance COMMAND INPUTS (plan lines
# 1916, 1918, 1770 enumerate the families), so their class is
# RegisteredCommandInput{command_contract_id} — which requires their
# command_contracts.toml row to exist first, and those rows were gated on the
# full family expansion (see command_contracts.toml's header and the
# tag-permanence rationale there; fgdb-5uw2 Phase B). A classification row
# minted before its contract row would either invent a contract id or carry a
# dangling one; both are the fail-open this registry exists to prevent. The
# first three rows below are exactly the creation-time population whose class
# was FORCED by the source without a contract id.
#
# PHASE B SEED (2026-08-02/03). The owner-confirmed v1 tag freeze opened the
# contract registry's Phase B (see command_contracts.toml's header). The F1/F2
# contract rows exist as of this increment, so the three F1/F2 command inputs
# below now carry their source-forced RegisteredCommandInput class with a
# resolvable contract id, landed in the same increment as their contract rows.
# The creation-time PreOrderArtifact population stands unchanged; the
# population only grows.
#
# F3 TRANCHE (2026-08-03) + FAMILY BINDING (round-12 T4). The fourteen F3
# attempt-lifecycle command inputs landed with their contract rows. An ARMED
# member (LocalAttemptRegistrationSpec, TxnOwnershipTransitionSpec,
# LocalOutcomeCompactionSpec) has one contract row per inner arm, so its
# classification row binds the MEMBER-ROOT id; the validator resolves a root
# iff a contract row carries an id of the form "{root}:{arm}" (family
# resolution — see registry_check::command_type_classification).
#
# ADJUDICATED NON-MEMBERS, recorded here so nobody re-derives them (the
# laws.toml pattern: record, never strike):
#   CatalogMutationSpec, SchemaMutationSpec, IndexDefinitionSpec,
#   IndexActivationSpec — plan lines 1770 and 1916 state outright that these
#   "are not command types" and line 1918 that "the synthetic
#   catalog/schema/index control types are forbidden"; no schema of these names
#   exists in any registry (MEASURED, 0 rows repo-wide). They are prose
#   prohibitions, not types, so they take NO classification row: a row would
#   itself mint the forbidden name into a registry. Ordinary DDL is a
#   transaction-carried `SchemaOp` (plan line 1918).
#   AuditVisibilityAdvanceSpec — named at plan line 1770 as "a Protocol-plane
#   maintenance input, never a `GlobalSequenceNeutralSpec`", and plan line 974
#   registers maintenance batch specs as command_contracts.toml Maintenance
#   rows; its class is therefore RegisteredCommandInput with a Maintenance
#   contract row — Phase B, not omittable and not landable yet.
#
# Row schema (closed; the loader rejects unknown keys before reading fields):
#   [[classification]]
#   type_name            = "..."      # stable type name, permanent, never reused
#   class                = "RegisteredCommandInput" | "PreOrderArtifact" |
#                          "CertificateInput" | "ResultOnly" |
#                          "ProjectionInput" | "RuntimeOnly"
#   command_contract_id  = "..."      # REQUIRED iff class = "RegisteredCommandInput";
#                                     # forbidden otherwise
#   source_location      = "aNN:LINE" # REQUIRED — the plan line that forces the class
#   statement            = "..."      # the forcing quote, verbatim or tightly excerpted
#   status               = "registered"
#
# registry_epoch changes only when rows are added or retired (plan line 290).
registry_epoch = 10

[[classification]]
type_name = "TxnSubmissionSpec"
class = "PreOrderArtifact"
source_location = "a10:1918"
statement = "TxnSubmissionSpec is not an ordered input: transaction submission is the pre-order construction pipeline that yields CommitCommand."
status = "registered"

# --- F5a checkpoint/floor/history-cut prefix. These are separately ordered
# --- inputs and therefore each binds one armless contract row.

[[classification]]
type_name = "CheckpointInstallSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:checkpoint-install-spec"
source_location = "a12:429"
statement = "CheckpointInstallSpec is the exact first retained command after its covered basis and installs the checkpoint, paired floor, and operational provisional cut as one authority transfer."
status = "registered"

[[classification]]
type_name = "InitialConfigFloorInstallSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:initial-config-floor-install-spec"
source_location = "a12:1966"
statement = "InitialConfigFloorInstallSpec is the sole local CheckpointInstalled -> Operational transition."
status = "registered"

[[classification]]
type_name = "HistoryCutActivationSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:history-cut-activation-spec"
source_location = "a12:1972"
statement = "HistoryCutActivationSpec is the sole local strengthening path and atomically replaces the provisional retention-map entry with the activated cut set."
status = "registered"

# --- F5b configuration transition. The four phase rows above are one closed
# --- family, so the classification binds the family member root.

[[classification]]
type_name = "ConfigurationTransitionSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:configuration-transition-spec"
source_location = "a10:1920"
statement = "ConfigurationTransitionSpec has exactly the source-ordered ProposeJoint, CommitJoint, CommitNew, and CommitRetirementFloor phases; every phase is a separate command."
status = "registered"

# --- F4 tranche (allocation escape and finalization). Armed families bind
# --- their member roots; the command-contract validator resolves those roots
# --- through the two exact arm rows under each shared outer tag.

[[classification]]
type_name = "AllocationReservationTransitionSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:allocation-reservation-transition-spec"
source_location = "a09:1900"
statement = "AllocationReservationTransitionSpec<Local> is the generated closed Local allocation transition union with exactly EscapingReserve and BindingCompaction inner tags."
status = "registered"

[[classification]]
type_name = "LocalFinalCertificationReserveSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-final-certification-reserve-spec"
source_location = "a09:1748"
statement = "LocalFinalCertificationReserveSpec is the exact Local final-certification reservation body."
status = "registered"

[[classification]]
type_name = "LocalFinalCertificationCancelSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-final-certification-cancel-spec"
source_location = "a09:1754"
statement = "LocalFinalCertificationCancelSpec names the exact Active reservation and NoTerminalPlanLockShareOrOrderProof before moving the reservation to CancelledBeforeSignature."
status = "registered"

[[classification]]
type_name = "FinalizationAllocationDispositionSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:finalization-allocation-disposition-spec"
source_location = "a09:1904"
statement = "FinalizationAllocationDispositionSpec<Local> has exactly the source-ordered Consumed and AbandonedSpent arms and no Meta instantiation."
status = "registered"

[[classification]]
type_name = "AuditExternalResolutionBatch"
class = "PreOrderArtifact"
source_location = "a07:1770"
statement = "AuditExternalResolutionBatch/receipt are pre-order evidence and AuditVisibilityAdvanceSpec is a Protocol-plane maintenance input, never a GlobalSequenceNeutralSpec."
status = "registered"

[[classification]]
type_name = "AuditExternalResolutionReceipt"
class = "PreOrderArtifact"
source_location = "a07:1770"
statement = "AuditExternalResolutionBatch/receipt are pre-order evidence and AuditVisibilityAdvanceSpec is a Protocol-plane maintenance input, never a GlobalSequenceNeutralSpec."
status = "registered"

# --- Phase B seed (F1/F2): the first RegisteredCommandInput rows, landed in
# --- the same increment as their command_contracts.toml contract rows.

[[classification]]
type_name = "RecoveryBridgeSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:recovery-bridge-spec"
source_location = "a19:2561"
statement = "RecoveryBridgeSpec<Role> is the exact role-specialized first-control body."
status = "registered"

[[classification]]
type_name = "LocalBeginReservationSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-begin-reservation-spec"
source_location = "a03:1482"
statement = "Apply creates LocalBeginReservationRecord, installs Reserved with the replay ref, and constructs TxnOutcomeRecord::BeginStaging; only that visible state permits BEGIN_ACCEPTED."
status = "registered"

[[classification]]
type_name = "LocalBeginTerminalSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-begin-terminal-spec"
source_location = "a03:1482"
statement = "LocalBeginTerminalSpec alone consumes Reserved and creates LocalNeverRegisteredRecord plus NeverRegisteredDetailed."
status = "registered"

# --- F3 tranche (attempt-lifecycle): fourteen RegisteredCommandInput rows,
# --- landed in the same increment as their contract rows. Armed members bind
# --- the member-root id (round-12 T4 family resolution).

[[classification]]
type_name = "LocalAttemptRegistrationSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-attempt-registration-spec"
source_location = "a03:1484"
statement = "LocalAttemptRegistrationSpec has the closed binding ExplicitBegin|Autocommit; apply changes explicit Reserved to BoundDetailed or creates the autocommit BoundDetailed lineage and constructs LocalAttemptRegistration."
status = "registered"

[[classification]]
type_name = "LocalAutocommitWriteSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-autocommit-write-spec"
source_location = "a03:1484"
statement = "Exactly one registered amendment: the fused single-statement autocommit write arm LocalAutocommitWriteSpec, whose coordinator-local permit verification and admission precede every observation while durable registration lands atomically with the commit."
status = "registered"

[[classification]]
type_name = "TxnOwnershipTransitionSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:txn-ownership-transition-spec"
source_location = "a10:1918"
statement = "Required Local semantic rows include TxnOwnershipTransitionSpec<Local>."
status = "registered"

[[classification]]
type_name = "TxnOwnershipExpiryAbortSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:txn-ownership-expiry-abort-spec"
source_location = "a10:1918"
statement = "Required Local semantic rows include TxnOwnershipExpiryAbortSpec<Local>."
status = "registered"

[[classification]]
type_name = "LocalStatementRegistrationSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-statement-registration-spec"
source_location = "a03:1486"
statement = "Apply byte-matches the current full binding, requires no Open statement, constructs LocalStatementRegistration, and inserts Open."
status = "registered"

[[classification]]
type_name = "LocalStatementPublicationSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-statement-publication-spec"
source_location = "a03:1488"
statement = "Buffered apply creates the role-valid applied publication reference, resource transfer, and independent-retention record; it creates ActiveDeliverable only through a legal inline activation, otherwise PendingVisibility."
status = "registered"

[[classification]]
type_name = "LocalStatementAbortSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-statement-abort-spec"
source_location = "a03:1488"
statement = "LocalStatementAbortSpec alone changes Open to Failed|Abandoned, resolves that statement ticket, proves no result/workspace effect, and applies the registration-pinned ContinueAttempt|AbortAttempt policy."
status = "registered"

[[classification]]
type_name = "LocalPrepareAdmissionSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-prepare-admission-spec"
source_location = "a03:1528"
statement = "Apply requires ReadWrite and no prior admission, constructs immutable LocalTerminalAdmissionFence::Armed, and atomically replaces the token's Detailed entry."
status = "registered"

[[classification]]
type_name = "LocalReadCloseSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-read-close-spec"
source_location = "a03:1534"
statement = "Apply advances LogicalCommandSeq/HLC but not CommitSeq, creates ReadClosed and, for autocommit, atomically detaches the result under the same pending/released/inline-active rule."
status = "registered"

[[classification]]
type_name = "LocalTerminalCompletionSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-terminal-completion-spec"
source_location = "a03:1532"
statement = "It monotonically adds verified bits, validates rather than manufactures the frozen public/capability artifacts, and creates TerminalReady only when the required bitmap is exactly satisfied."
status = "registered"

[[classification]]
type_name = "TxnAbortSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:txn-abort-spec"
source_location = "a03:1536"
statement = "TxnAbortSpec is only the active/unadmitted lifecycle path; its terminal-abort authority must use the registered LifecycleOperation::RequestTerminalAbort{stage:Active}, and it can never consume a PrepareAdmitted fence or final-certification reservation."
status = "registered"

[[classification]]
type_name = "LocalOutcomeCompactionSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-outcome-compaction-spec"
source_location = "a03:1540"
statement = "Apply requires the summary leaf copy the exact frozen public bytes/receipt, writes the authenticated leaf, and atomically changes TxnOutcomeIndex Detailed to Compacted together with the begin-index compact transition."
status = "registered"

[[classification]]
type_name = "LocalOutcomeExpirySpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-outcome-expiry-spec"
source_location = "a03:1540"
statement = "LocalOutcomeExpirySpec is the distinct ordered transition; apply atomically changes both indexes to authenticated ExpiredTombstone values and permanently prevents key/token reuse."
status = "registered"

[[classification]]
type_name = "LocalConflictCompactionSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-conflict-compaction-spec"
source_location = "a03:1480"
statement = "LocalConflictCompactionSpec may replace detail only when every possible future transaction has a start/snapshot beyond the selected entry's closed overlap interval and no live witness can add an edge."
status = "registered"

# --- F5c durable-format transition (frozen ordinal 26). An ARMED member, so the
# --- row binds the MEMBER-ROOT id and the validator resolves it through family
# --- resolution against the five "{root}:{arm}" contract rows (round-12 T4).
# --- The canonical spelling is FormatTransitionSpec<Role> per ruling I-6; L1218's
# --- closing "Local uses LocalFormatTransitionSpec" is the dual spelling that
# --- ruling resolved, and the alias is deliberately NOT minted as a second row.

[[classification]]
type_name = "FormatTransitionSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:format-transition-spec"
source_location = "a02:1218"
statement = "FormatTransitionSpec<Role> is the closed Semantic ordered union AdvertiseTarget; ActivateWriteEpoch; RecordFirstTargetWrite; RewriteEquivalent; RetireOldDecoder. ActivateWriteEpoch contains no guessed future position; its applied command record supplies the assigned activation_applied_position, which every later activation reference authenticates."
status = "registered"

# --- F5d delta-batch retention cut (frozen ordinal 27). C2 (round 10) included
# --- it as the last F5 member; L1930 spells the command and its evidence but
# --- never the struct body, so the contract row carries T6 sentinels.

[[classification]]
type_name = "LocalDeltaBatchRetentionCutSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-delta-batch-retention-cut-spec"
source_location = "a10:1930"
statement = "The role-specific ordered LocalDeltaBatchRetentionCutSpec may install ActivatedDeltaRetentionCut<Local> and advance retained_after_commit_seq only after the paired checkpoint/floor and ordinary activated retention cut cover the same position; apply replaces old strong entries with the prefix commitment in the same state transition."
status = "registered"

# --- F6 remote-retention-and-trust (frozen ordinals 28-30). The union row is
# --- ARMED, so it binds the member-root id and resolves through the eight
# --- "{root}:{arm}" contract rows (round-12 T4). The four Remote*Spec
# --- sub-bodies its arms name are PreOrderArtifact per ruling C11: L1582
# --- reaches each via StrongRef inside an arm payload, which is the pre-order
# --- construction shape, never a second ordered input.

[[classification]]
type_name = "RemoteRetentionControlSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:remote-retention-control-spec"
source_location = "a04:1582"
statement = "RemoteRetentionControlSpec is the closed role-valid union AcquireGrant; RegisterConsumerGrant; RequestConsumerRelease; PublishConsumerReleaseEvidence; ApplyAuthorityRelease; PublishAuthorityReleaseAck; ConsumeReleaseAck; AdoptLegacyAuthorityTransfer, included in Local/Meta/Shard control registries; the generated role matrix fixes each arm to authority, consumer, or transition-consumer posture and there is no generic transfer arm."
status = "registered"

[[classification]]
type_name = "AdvanceRemoteConfigurationEvidenceSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:advance-remote-configuration-evidence-spec"
source_location = "a04:1584"
statement = "AdvanceRemoteConfigurationEvidenceSpec is the ordinary role-valid CAS; apply verifies the exact predecessor/anchor and atomically selects CurrentEvidence(successor,generation+1), and topology/configuration can never advance while trust remains behind."
status = "registered"

[[classification]]
type_name = "ValidateRemoteConfigurationAnchorSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:validate-remote-configuration-anchor-spec"
source_location = "a04:1586"
statement = "ValidateRemoteConfigurationAnchorSpec constructs ValidatedRemoteConfigurationAnchor and atomically selects its strong anchor at generation+1, legal only after the RemoteTrustCompactionPrecondition proves no surviving object still binds a predecessor evidence; both specs are explicit Local/Meta/Shard control variants."
status = "registered"

[[classification]]
type_name = "RemoteRetentionGrantSpec"
class = "PreOrderArtifact"
source_location = "a04:1582"
statement = "C11: the AcquireGrant arm reaches RemoteRetentionGrantSpec via StrongRef<RemoteRetentionGrantSpec>, the pre-order body shape; the ordered input is RemoteRetentionControlSpec."
status = "registered"

[[classification]]
type_name = "RemoteRetentionReleaseRequestSpec"
class = "PreOrderArtifact"
source_location = "a04:1582"
statement = "C11: the RequestConsumerRelease arm reaches RemoteRetentionReleaseRequestSpec via StrongRef<RemoteRetentionReleaseRequestSpec>, the pre-order body shape; the ordered input is RemoteRetentionControlSpec."
status = "registered"

[[classification]]
type_name = "RemoteRetentionReleaseApplySpec"
class = "PreOrderArtifact"
source_location = "a04:1582"
statement = "C11: the ApplyAuthorityRelease arm reaches RemoteRetentionReleaseApplySpec via StrongRef<RemoteRetentionReleaseApplySpec>, the pre-order body shape; the ordered input is RemoteRetentionControlSpec."
status = "registered"

[[classification]]
type_name = "RetentionAuthorityTransferAdoptionSpec"
class = "PreOrderArtifact"
source_location = "a04:1582"
statement = "C11: the AdoptLegacyAuthorityTransfer arm reaches RetentionAuthorityTransferAdoptionSpec via StrongRef<RetentionAuthorityTransferAdoptionSpec>, the pre-order body shape; the ordered input is RemoteRetentionControlSpec."
status = "registered"

# --- F7 branches-and-merge (frozen ordinals 31-40). All ten are armless rows;
# --- MergePrepareSpec is ordered semantic per ruling I-5 (L457 controls, and
# --- L2033's "Pre-order" is the benign built-before-ordering sense).

[[classification]]
type_name = "BranchEpochBoundaryReserveSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:branch-epoch-boundary-reserve-spec"
source_location = "a13:2002"
statement = "BranchEpochBoundaryReserveSpec is ordered first; apply validates current state and creates the immutable Active BranchEpochBoundaryReservation whose exclusive hold root every branch mutation and key transition consults, so expensive key closures never depend on a future install sequence."
status = "registered"

[[classification]]
type_name = "BranchForkSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:branch-fork-spec"
source_location = "a13:2031"
statement = "BranchForkSpec<Role:AuthorityOwningRole> consumes the Active boundary reservation and a complete pre-built fork bundle; deterministic apply creates no key material and atomically installs the already-known manifests/registries, consuming the reservation and releasing its holds."
status = "registered"

[[classification]]
type_name = "BranchGrantSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:branch-grant-spec"
source_location = "a13:2031"
statement = "BranchGrantSpec<Role:AuthorityOwningRole> is the analogous grant install over BranchGrantBundle; snapshot issue and revocation encode close, frozen root, then double rotation, and a published grant or revocation forces recovery to complete the matching install."
status = "registered"

[[classification]]
type_name = "BranchEpochBoundaryCancelSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:branch-epoch-boundary-cancel-spec"
source_location = "a13:2031"
statement = "BranchEpochBoundaryCancelSpec is legal before any bundle, prepared owner, certificate, or wrap exists and requires NoBoundaryClosureProof."
status = "registered"

[[classification]]
type_name = "BranchEpochBoundaryAbandonSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:branch-epoch-boundary-abandon-spec"
source_location = "a13:2031"
statement = "BranchEpochBoundaryAbandonSpec is legal after construction only with complete signer/prepared closure, no install order/candidate/external grant publication, and typed cleanup/zeroization evidence."
status = "registered"

[[classification]]
type_name = "BranchRetireSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:branch-retire-spec"
source_location = "a13:465"
statement = "BranchRetireSpec is an ordinary semantic control command with its own terminal audit gate and command_contracts.toml row; it atomically installs a permanent BranchRetireTombstone, fails closed on unknown branch, stale head, epoch mismatch, or an already-installed tombstone, and exact retry is idempotent."
status = "registered"

[[classification]]
type_name = "BranchRetireFinalizeSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:branch-retire-finalize-spec"
source_location = "a13:467"
statement = "Phase two is gated on drain that is proved, never waited out; only then may BranchRetireFinalizeSpec activate the branch's retention cut and enter the branch's envelope root and write-key epochs into the staged key-destruction pipeline."
status = "registered"

[[classification]]
type_name = "MergeRejectSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:merge-reject-spec"
source_location = "a13:457"
statement = "A deterministic pre-effect rejection uses a typed MergeRejectSpec control command to install Rejected with a stable reason digest."
status = "registered"

[[classification]]
type_name = "MergePrepareSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:merge-prepare-spec"
source_location = "a13:457"
statement = "The graph-effect-free MergePrepareSpec durably maps the caller/target-scoped nonce to the preidentified stable token and exact canonical capability bytes; ordered semantic per ruling I-5 — L457 controls and L2033's Pre-order is built-before-ordering, not the pre-order-evidence exclusion."
status = "registered"

[[classification]]
type_name = "MergeExecuteSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:merge-execute-spec"
source_location = "a13:1012"
statement = "MergeExecuteSpec strongly carries the Execute-arm MergeCapabilityOperationAuthorizationRecord before evaluation or charging; neither a ledger token nor a status handle can replace it."
status = "registered"

# --- F8 id-validtime-constraint-resource-escrow (frozen ordinals 41-43).
# --- EscrowRightsTransitionSpec is ARMED, so its row binds the member-root id
# --- (round-12 T4). Valid-time and constraint contribute ZERO rows per I-9;
# --- IdRangeGrantSpec stays excluded as pre-order (L1892).

[[classification]]
type_name = "ResourceLedgerTransitionSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:resource-ledger-transition-spec"
source_location = "a12:1994"
statement = "ResourceLedgerTransitionSpec is the standalone-control wrapper over sorted transition refs with a complete transition-id-set commitment; transactions and exact lifecycle specs embed transition refs directly, and a generated enclosing-command matrix fixes legal operation tags."
status = "registered"

[[classification]]
type_name = "EscrowRightsTransitionSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:escrow-rights-transition-spec"
source_location = "a12:256"
statement = "EscrowRightsTransitionSpec is the closed Local union TransferAtomic; ReconfigureDomain; AdvanceQuotaEpoch; RetireBranchOverlay; CompactTerminalEvidence. Epoch quota reset carries subject-bound rollback-resistant TimeValidationEvidence, RetireBranchOverlay is orderable only from the BranchRetireSpec capsule, and CompactTerminalEvidence neither mints nor burns rights."
status = "registered"

[[classification]]
type_name = "ExpiryEpochAdvanceSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:expiry-epoch-advance-spec"
source_location = "a12:266"
statement = "The sequenced command stream observes expiry only as one registered ExpiryEpochAdvanceSpec control command per configured epoch tick — the sole sequencer-visible expiry artifact, an ordered transition carrying subject-bound rollback-resistant TimeValidationEvidence; included per ruling C4, with L268 deferring exactly this row decision to G0."
status = "registered"
