# =============================================================================
# command_type_classification.toml — input-looking type classification (G0)
# =============================================================================
# Bead: fgdb-5uw2
# Provenance: plan §5.1 (COMPREHENSIVE_PLAN_FOR_THE_DESIGN_OF_FRANKENGRAPHDB.md
# line 294), verbatim: "The paired `command_type_classification.toml` assigns
# every independently addressable normative input-looking type—including every
# type whose stable name ends in `Spec`—exactly one class:
# `RegisteredCommandInput{command_contract_id}`, `PreOrderArtifact`,
# `CertificateInput`, `ResultOnly`, `ProjectionInput`, or `RuntimeOnly`.
# A suffix conveys no semantics."
#
# WHY THE CREATION POPULATION WAS THREE ROWS, NOT ~40+. MEASURED 2026-07-28 at
# HEAD 5d42e28: registries/appendix_a_catalog.toml carries 39 logical kinds
# whose stable name ends in Spec, plus further Spec-named top-level candidates.
# Nearly all of them are semantic or maintenance COMMAND INPUTS (plan lines
# 1916, 1918, 1770 enumerate the families), so their class is
# RegisteredCommandInput{command_contract_id} — which requires their
# command_contracts.toml row to exist first, and those rows were gated on the
# full family expansion (see command_contracts.toml's header and the
# tag-permanence rationale there; fgdb-5uw2 Phase B). A classification row
# minted before its contract row would either invent a contract id or carry a
# dangling one; both are the fail-open this registry exists to prevent. The
# first three rows below are exactly the creation-time population whose class
# was FORCED by the source without a contract id.
#
# PHASE B SEED (2026-08-02/03). The owner-confirmed v1 tag freeze opened the
# contract registry's Phase B (see command_contracts.toml's header). The F1/F2
# contract rows exist as of this increment, so the three F1/F2 command inputs
# below now carry their source-forced RegisteredCommandInput class with a
# resolvable contract id, landed in the same increment as their contract rows.
# The creation-time PreOrderArtifact population stands unchanged; the
# population only grows.
#
# F3 TRANCHE (2026-08-03) + FAMILY BINDING (round-12 T4). The fourteen F3
# attempt-lifecycle command inputs landed with their contract rows. An ARMED
# member (LocalAttemptRegistrationSpec, TxnOwnershipTransitionSpec,
# LocalOutcomeCompactionSpec) has one contract row per inner arm, so its
# classification row binds the MEMBER-ROOT id; the validator resolves a root
# iff a contract row carries an id of the form "{root}:{arm}" (family
# resolution — see registry_check::command_type_classification).
#
# ADJUDICATED NON-MEMBERS, recorded here so nobody re-derives them (the
# laws.toml pattern: record, never strike):
#   CatalogMutationSpec, SchemaMutationSpec, IndexDefinitionSpec,
#   IndexActivationSpec — plan lines 1770 and 1916 state outright that these
#   "are not command types" and line 1918 that "the synthetic
#   catalog/schema/index control types are forbidden"; no schema of these names
#   exists in any registry (MEASURED, 0 rows repo-wide). They are prose
#   prohibitions, not types, so they take NO classification row: a row would
#   itself mint the forbidden name into a registry. Ordinary DDL is a
#   transaction-carried `SchemaOp` (plan line 1918).
#   AuditVisibilityAdvanceSpec — named at plan line 1770 as "a Protocol-plane
#   maintenance input, never a `GlobalSequenceNeutralSpec`", and plan line 974
#   registers maintenance batch specs as command_contracts.toml Maintenance
#   rows; its class is therefore RegisteredCommandInput with a Maintenance
#   contract row — Phase B, not omittable and not landable yet.
#
# Row schema (closed; the loader rejects unknown keys before reading fields):
#   [[classification]]
#   type_name            = "..."      # stable type name, permanent, never reused
#   class                = "RegisteredCommandInput" | "PreOrderArtifact" |
#                          "CertificateInput" | "ResultOnly" |
#                          "ProjectionInput" | "RuntimeOnly"
#   command_contract_id  = "..."      # REQUIRED iff class = "RegisteredCommandInput";
#                                     # forbidden otherwise
#   source_location      = "aNN:LINE" # REQUIRED — the plan line that forces the class
#   statement            = "..."      # the forcing quote, verbatim or tightly excerpted
#   status               = "registered"
#
# registry_epoch changes only when rows are added or retired (plan line 290).
registry_epoch = 21

[[classification]]
type_name = "TxnSubmissionSpec"
class = "PreOrderArtifact"
source_location = "a10:1918"
statement = "TxnSubmissionSpec is not an ordered input: transaction submission is the pre-order construction pipeline that yields CommitCommand."
status = "registered"

# --- F14 semantic GC authorization (frozen ordinals 66-69). GcProposal is
# --- pre-order evidence, while physical initialize/dispatch/status/receipt
# --- work is Protocol maintenance. Only these four exact ordered Local
# --- Semantic inputs gain RegisteredCommandInput rows; the disposition import
# --- has two contract arms under its one classified member root.

[[classification]]
type_name = "LocalGcAuthorizeSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-gc-authorize-spec"
source_location = "a14:2045"
statement = "LocalGcAuthorizeSpec is the ordered Local semantic authorization carrying GcProposal; followers recompute the candidate and apply exactly one stable GcDecisionRecord, while an Accepted decision alone never authorizes physical work."
status = "registered"

[[classification]]
type_name = "LocalGcApplyQuarantineSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-gc-apply-quarantine-spec"
source_location = "a14:2047"
statement = "LocalGcApplyQuarantineSpec consumes an Accepted decision plus exact current-root/reachability/disjointness and plan-family witnesses; Semantic apply installs LocalGcReclaimAuthorizationRecord and Quarantined state only, never a Protocol operation."
status = "registered"

[[classification]]
type_name = "LocalGcCancellationAuthorizeSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-gc-cancellation-authorize-spec"
source_location = "a14:2055"
statement = "LocalGcCancellationAuthorizeSpec requires complete Protocol NoStartFenced evidence, the exact member-target-family no-start bijection, unchanged physical roots, and the terminal audit gate; apply creates the standalone Local cancellation authority without importing disposition."
status = "registered"

[[classification]]
type_name = "GcPhysicalDispositionImportSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:gc-physical-disposition-import-spec"
source_location = "a14:2055"
statement = "GcPhysicalDispositionImportSpec<Local> is the source-ordered Completed|Cancelled Semantic union; only it imports portable terminal evidence into PhysicalCompletedPendingCut or CancelledBeforePhysicalStart, and the Cancelled arm requires exact LocalStandalone cancellation authority."
status = "registered"

# --- F5a checkpoint/floor/history-cut prefix. These are separately ordered
# --- inputs and therefore each binds one armless contract row.

[[classification]]
type_name = "CheckpointInstallSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:checkpoint-install-spec"
source_location = "a12:429"
statement = "CheckpointInstallSpec is the exact first retained command after its covered basis and installs the checkpoint, paired floor, and operational provisional cut as one authority transfer."
status = "registered"

[[classification]]
type_name = "InitialConfigFloorInstallSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:initial-config-floor-install-spec"
source_location = "a12:1966"
statement = "InitialConfigFloorInstallSpec is the sole local CheckpointInstalled -> Operational transition."
status = "registered"

[[classification]]
type_name = "HistoryCutActivationSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:history-cut-activation-spec"
source_location = "a12:1972"
statement = "HistoryCutActivationSpec is the sole local strengthening path and atomically replaces the provisional retention-map entry with the activated cut set."
status = "registered"

# --- F5b configuration transition. The four phase rows above are one closed
# --- family, so the classification binds the family member root.

[[classification]]
type_name = "ConfigurationTransitionSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:configuration-transition-spec"
source_location = "a10:1920"
statement = "ConfigurationTransitionSpec has exactly the source-ordered ProposeJoint, CommitJoint, CommitNew, and CommitRetirementFloor phases; every phase is a separate command."
status = "registered"

# --- F4 tranche (allocation escape and finalization). Armed families bind
# --- their member roots; the command-contract validator resolves those roots
# --- through the two exact arm rows under each shared outer tag.

[[classification]]
type_name = "AllocationReservationTransitionSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:allocation-reservation-transition-spec"
source_location = "a09:1900"
statement = "AllocationReservationTransitionSpec<Local> is the generated closed Local allocation transition union with exactly EscapingReserve and BindingCompaction inner tags."
status = "registered"

[[classification]]
type_name = "LocalFinalCertificationReserveSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-final-certification-reserve-spec"
source_location = "a09:1748"
statement = "LocalFinalCertificationReserveSpec is the exact Local final-certification reservation body."
status = "registered"

[[classification]]
type_name = "LocalFinalCertificationCancelSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-final-certification-cancel-spec"
source_location = "a09:1754"
statement = "LocalFinalCertificationCancelSpec names the exact Active reservation and NoTerminalPlanLockShareOrOrderProof before moving the reservation to CancelledBeforeSignature."
status = "registered"

[[classification]]
type_name = "FinalizationAllocationDispositionSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:finalization-allocation-disposition-spec"
source_location = "a09:1904"
statement = "FinalizationAllocationDispositionSpec<Local> has exactly the source-ordered Consumed and AbandonedSpent arms and no Meta instantiation."
status = "registered"

[[classification]]
type_name = "AuditExternalResolutionBatch"
class = "PreOrderArtifact"
source_location = "a07:1770"
statement = "AuditExternalResolutionBatch/receipt are pre-order evidence and AuditVisibilityAdvanceSpec is a Protocol-plane maintenance input, never a GlobalSequenceNeutralSpec."
status = "registered"

[[classification]]
type_name = "AuditExternalResolutionReceipt"
class = "PreOrderArtifact"
source_location = "a07:1770"
statement = "AuditExternalResolutionBatch/receipt are pre-order evidence and AuditVisibilityAdvanceSpec is a Protocol-plane maintenance input, never a GlobalSequenceNeutralSpec."
status = "registered"

# --- Phase B seed (F1/F2): the first RegisteredCommandInput rows, landed in
# --- the same increment as their command_contracts.toml contract rows.

[[classification]]
type_name = "RecoveryBridgeSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:recovery-bridge-spec"
source_location = "a19:2561"
statement = "RecoveryBridgeSpec<Role> is the exact role-specialized first-control body."
status = "registered"

[[classification]]
type_name = "LocalBeginReservationSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-begin-reservation-spec"
source_location = "a03:1482"
statement = "Apply creates LocalBeginReservationRecord, installs Reserved with the replay ref, and constructs TxnOutcomeRecord::BeginStaging; only that visible state permits BEGIN_ACCEPTED."
status = "registered"

[[classification]]
type_name = "LocalBeginTerminalSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-begin-terminal-spec"
source_location = "a03:1482"
statement = "LocalBeginTerminalSpec alone consumes Reserved and creates LocalNeverRegisteredRecord plus NeverRegisteredDetailed."
status = "registered"

# --- F3 tranche (attempt-lifecycle): fourteen RegisteredCommandInput rows,
# --- landed in the same increment as their contract rows. Armed members bind
# --- the member-root id (round-12 T4 family resolution).

[[classification]]
type_name = "LocalAttemptRegistrationSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-attempt-registration-spec"
source_location = "a03:1484"
statement = "LocalAttemptRegistrationSpec has the closed binding ExplicitBegin|Autocommit; apply changes explicit Reserved to BoundDetailed or creates the autocommit BoundDetailed lineage and constructs LocalAttemptRegistration."
status = "registered"

[[classification]]
type_name = "LocalAutocommitWriteSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-autocommit-write-spec"
source_location = "a03:1484"
statement = "Exactly one registered amendment: the fused single-statement autocommit write arm LocalAutocommitWriteSpec, whose coordinator-local permit verification and admission precede every observation while durable registration lands atomically with the commit."
status = "registered"

[[classification]]
type_name = "TxnOwnershipTransitionSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:txn-ownership-transition-spec"
source_location = "a10:1918"
statement = "Required Local semantic rows include TxnOwnershipTransitionSpec<Local>."
status = "registered"

[[classification]]
type_name = "TxnOwnershipExpiryAbortSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:txn-ownership-expiry-abort-spec"
source_location = "a10:1918"
statement = "Required Local semantic rows include TxnOwnershipExpiryAbortSpec<Local>."
status = "registered"

[[classification]]
type_name = "LocalStatementRegistrationSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-statement-registration-spec"
source_location = "a03:1486"
statement = "Apply byte-matches the current full binding, requires no Open statement, constructs LocalStatementRegistration, and inserts Open."
status = "registered"

[[classification]]
type_name = "LocalStatementPublicationSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-statement-publication-spec"
source_location = "a03:1488"
statement = "Buffered apply creates the role-valid applied publication reference, resource transfer, and independent-retention record; it creates ActiveDeliverable only through a legal inline activation, otherwise PendingVisibility."
status = "registered"

[[classification]]
type_name = "LocalStatementAbortSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-statement-abort-spec"
source_location = "a03:1488"
statement = "LocalStatementAbortSpec alone changes Open to Failed|Abandoned, resolves that statement ticket, proves no result/workspace effect, and applies the registration-pinned ContinueAttempt|AbortAttempt policy."
status = "registered"

[[classification]]
type_name = "LocalPrepareAdmissionSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-prepare-admission-spec"
source_location = "a03:1528"
statement = "Apply requires ReadWrite and no prior admission, constructs immutable LocalTerminalAdmissionFence::Armed, and atomically replaces the token's Detailed entry."
status = "registered"

[[classification]]
type_name = "LocalReadCloseSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-read-close-spec"
source_location = "a03:1534"
statement = "Apply advances LogicalCommandSeq/HLC but not CommitSeq, creates ReadClosed and, for autocommit, atomically detaches the result under the same pending/released/inline-active rule."
status = "registered"

[[classification]]
type_name = "LocalTerminalCompletionSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-terminal-completion-spec"
source_location = "a03:1532"
statement = "It monotonically adds verified bits, validates rather than manufactures the frozen public/capability artifacts, and creates TerminalReady only when the required bitmap is exactly satisfied."
status = "registered"

[[classification]]
type_name = "TxnAbortSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:txn-abort-spec"
source_location = "a03:1536"
statement = "TxnAbortSpec is only the active/unadmitted lifecycle path; its terminal-abort authority must use the registered LifecycleOperation::RequestTerminalAbort{stage:Active}, and it can never consume a PrepareAdmitted fence or final-certification reservation."
status = "registered"

[[classification]]
type_name = "LocalOutcomeCompactionSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-outcome-compaction-spec"
source_location = "a03:1540"
statement = "Apply requires the summary leaf copy the exact frozen public bytes/receipt, writes the authenticated leaf, and atomically changes TxnOutcomeIndex Detailed to Compacted together with the begin-index compact transition."
status = "registered"

[[classification]]
type_name = "LocalOutcomeExpirySpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-outcome-expiry-spec"
source_location = "a03:1540"
statement = "LocalOutcomeExpirySpec is the distinct ordered transition; apply atomically changes both indexes to authenticated ExpiredTombstone values and permanently prevents key/token reuse."
status = "registered"

[[classification]]
type_name = "LocalConflictCompactionSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-conflict-compaction-spec"
source_location = "a03:1480"
statement = "LocalConflictCompactionSpec may replace detail only when every possible future transaction has a start/snapshot beyond the selected entry's closed overlap interval and no live witness can add an edge."
status = "registered"

# --- F5c durable-format transition (frozen ordinal 26). An ARMED member, so the
# --- row binds the MEMBER-ROOT id and the validator resolves it through family
# --- resolution against the five "{root}:{arm}" contract rows (round-12 T4).
# --- The canonical spelling is FormatTransitionSpec<Role> per ruling I-6; L1218's
# --- closing "Local uses LocalFormatTransitionSpec" is the dual spelling that
# --- ruling resolved, and the alias is deliberately NOT minted as a second row.

[[classification]]
type_name = "FormatTransitionSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:format-transition-spec"
source_location = "a02:1218"
statement = "FormatTransitionSpec<Role> is the closed Semantic ordered union AdvertiseTarget; ActivateWriteEpoch; RecordFirstTargetWrite; RewriteEquivalent; RetireOldDecoder. ActivateWriteEpoch contains no guessed future position; its applied command record supplies the assigned activation_applied_position, which every later activation reference authenticates."
status = "registered"

# --- F5d delta-batch retention cut (frozen ordinal 27). C2 (round 10) included
# --- it as the last F5 member; L1930 spells the command and its evidence but
# --- never the struct body, so the contract row carries T6 sentinels.

[[classification]]
type_name = "LocalDeltaBatchRetentionCutSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-delta-batch-retention-cut-spec"
source_location = "a10:1930"
statement = "The role-specific ordered LocalDeltaBatchRetentionCutSpec may install ActivatedDeltaRetentionCut<Local> and advance retained_after_commit_seq only after the paired checkpoint/floor and ordinary activated retention cut cover the same position; apply replaces old strong entries with the prefix commitment in the same state transition."
status = "registered"

# --- F6 remote-retention-and-trust (frozen ordinals 28-30). The union row is
# --- ARMED, so it binds the member-root id and resolves through the eight
# --- "{root}:{arm}" contract rows (round-12 T4). The four Remote*Spec
# --- sub-bodies its arms name are PreOrderArtifact per ruling C11: L1582
# --- reaches each via StrongRef inside an arm payload, which is the pre-order
# --- construction shape, never a second ordered input.

[[classification]]
type_name = "RemoteRetentionControlSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:remote-retention-control-spec"
source_location = "a04:1582"
statement = "RemoteRetentionControlSpec is the closed role-valid union AcquireGrant; RegisterConsumerGrant; RequestConsumerRelease; PublishConsumerReleaseEvidence; ApplyAuthorityRelease; PublishAuthorityReleaseAck; ConsumeReleaseAck; AdoptLegacyAuthorityTransfer, included in Local/Meta/Shard control registries; the generated role matrix fixes each arm to authority, consumer, or transition-consumer posture and there is no generic transfer arm."
status = "registered"

[[classification]]
type_name = "AdvanceRemoteConfigurationEvidenceSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:advance-remote-configuration-evidence-spec"
source_location = "a04:1584"
statement = "AdvanceRemoteConfigurationEvidenceSpec is the ordinary role-valid CAS; apply verifies the exact predecessor/anchor and atomically selects CurrentEvidence(successor,generation+1), and topology/configuration can never advance while trust remains behind."
status = "registered"

[[classification]]
type_name = "ValidateRemoteConfigurationAnchorSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:validate-remote-configuration-anchor-spec"
source_location = "a04:1586"
statement = "ValidateRemoteConfigurationAnchorSpec constructs ValidatedRemoteConfigurationAnchor and atomically selects its strong anchor at generation+1, legal only after the RemoteTrustCompactionPrecondition proves no surviving object still binds a predecessor evidence; both specs are explicit Local/Meta/Shard control variants."
status = "registered"

[[classification]]
type_name = "RemoteRetentionGrantSpec"
class = "PreOrderArtifact"
source_location = "a04:1582"
statement = "C11: the AcquireGrant arm reaches RemoteRetentionGrantSpec via StrongRef<RemoteRetentionGrantSpec>, the pre-order body shape; the ordered input is RemoteRetentionControlSpec."
status = "registered"

[[classification]]
type_name = "RemoteRetentionReleaseRequestSpec"
class = "PreOrderArtifact"
source_location = "a04:1582"
statement = "C11: the RequestConsumerRelease arm reaches RemoteRetentionReleaseRequestSpec via StrongRef<RemoteRetentionReleaseRequestSpec>, the pre-order body shape; the ordered input is RemoteRetentionControlSpec."
status = "registered"

[[classification]]
type_name = "RemoteRetentionReleaseApplySpec"
class = "PreOrderArtifact"
source_location = "a04:1582"
statement = "C11: the ApplyAuthorityRelease arm reaches RemoteRetentionReleaseApplySpec via StrongRef<RemoteRetentionReleaseApplySpec>, the pre-order body shape; the ordered input is RemoteRetentionControlSpec."
status = "registered"

[[classification]]
type_name = "RetentionAuthorityTransferAdoptionSpec"
class = "PreOrderArtifact"
source_location = "a04:1582"
statement = "C11: the AdoptLegacyAuthorityTransfer arm reaches RetentionAuthorityTransferAdoptionSpec via StrongRef<RetentionAuthorityTransferAdoptionSpec>, the pre-order body shape; the ordered input is RemoteRetentionControlSpec."
status = "registered"

# --- F7 branches-and-merge (frozen ordinals 31-40). All ten are armless rows;
# --- MergePrepareSpec is ordered semantic per ruling I-5 (L457 controls, and
# --- L2033's "Pre-order" is the benign built-before-ordering sense).

[[classification]]
type_name = "BranchEpochBoundaryReserveSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:branch-epoch-boundary-reserve-spec"
source_location = "a13:2002"
statement = "BranchEpochBoundaryReserveSpec is ordered first; apply validates current state and creates the immutable Active BranchEpochBoundaryReservation whose exclusive hold root every branch mutation and key transition consults, so expensive key closures never depend on a future install sequence."
status = "registered"

[[classification]]
type_name = "BranchForkSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:branch-fork-spec"
source_location = "a13:2031"
statement = "BranchForkSpec<Role:AuthorityOwningRole> consumes the Active boundary reservation and a complete pre-built fork bundle; deterministic apply creates no key material and atomically installs the already-known manifests/registries, consuming the reservation and releasing its holds."
status = "registered"

[[classification]]
type_name = "BranchGrantSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:branch-grant-spec"
source_location = "a13:2031"
statement = "BranchGrantSpec<Role:AuthorityOwningRole> is the analogous grant install over BranchGrantBundle; snapshot issue and revocation encode close, frozen root, then double rotation, and a published grant or revocation forces recovery to complete the matching install."
status = "registered"

[[classification]]
type_name = "BranchEpochBoundaryCancelSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:branch-epoch-boundary-cancel-spec"
source_location = "a13:2031"
statement = "BranchEpochBoundaryCancelSpec is legal before any bundle, prepared owner, certificate, or wrap exists and requires NoBoundaryClosureProof."
status = "registered"

[[classification]]
type_name = "BranchEpochBoundaryAbandonSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:branch-epoch-boundary-abandon-spec"
source_location = "a13:2031"
statement = "BranchEpochBoundaryAbandonSpec is legal after construction only with complete signer/prepared closure, no install order/candidate/external grant publication, and typed cleanup/zeroization evidence."
status = "registered"

[[classification]]
type_name = "BranchRetireSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:branch-retire-spec"
source_location = "a13:465"
statement = "BranchRetireSpec is an ordinary semantic control command with its own terminal audit gate and command_contracts.toml row; it atomically installs a permanent BranchRetireTombstone, fails closed on unknown branch, stale head, epoch mismatch, or an already-installed tombstone, and exact retry is idempotent."
status = "registered"

[[classification]]
type_name = "BranchRetireFinalizeSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:branch-retire-finalize-spec"
source_location = "a13:467"
statement = "Phase two is gated on drain that is proved, never waited out; only then may BranchRetireFinalizeSpec activate the branch's retention cut and enter the branch's envelope root and write-key epochs into the staged key-destruction pipeline."
status = "registered"

[[classification]]
type_name = "MergeRejectSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:merge-reject-spec"
source_location = "a13:457"
statement = "A deterministic pre-effect rejection uses a typed MergeRejectSpec control command to install Rejected with a stable reason digest."
status = "registered"

[[classification]]
type_name = "MergePrepareSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:merge-prepare-spec"
source_location = "a13:457"
statement = "The graph-effect-free MergePrepareSpec durably maps the caller/target-scoped nonce to the preidentified stable token and exact canonical capability bytes; ordered semantic per ruling I-5 — L457 controls and L2033's Pre-order is built-before-ordering, not the pre-order-evidence exclusion."
status = "registered"

[[classification]]
type_name = "MergeExecuteSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:merge-execute-spec"
source_location = "a13:1012"
statement = "MergeExecuteSpec strongly carries the Execute-arm MergeCapabilityOperationAuthorizationRecord before evaluation or charging; neither a ledger token nor a status handle can replace it."
status = "registered"

# --- F8 id-validtime-constraint-resource-escrow (frozen ordinals 41-43).
# --- EscrowRightsTransitionSpec is ARMED, so its row binds the member-root id
# --- (round-12 T4). Valid-time and constraint contribute ZERO rows per I-9;
# --- IdRangeGrantSpec stays excluded as pre-order (L1892).

[[classification]]
type_name = "ResourceLedgerTransitionSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:resource-ledger-transition-spec"
source_location = "a12:1994"
statement = "ResourceLedgerTransitionSpec is the standalone-control wrapper over sorted transition refs with a complete transition-id-set commitment; transactions and exact lifecycle specs embed transition refs directly, and a generated enclosing-command matrix fixes legal operation tags."
status = "registered"

[[classification]]
type_name = "EscrowRightsTransitionSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:escrow-rights-transition-spec"
source_location = "a12:256"
statement = "EscrowRightsTransitionSpec is the closed Local union TransferAtomic; ReconfigureDomain; AdvanceQuotaEpoch; RetireBranchOverlay; CompactTerminalEvidence. Epoch quota reset carries subject-bound rollback-resistant TimeValidationEvidence, RetireBranchOverlay is orderable only from the BranchRetireSpec capsule, and CompactTerminalEvidence neither mints nor burns rights."
status = "registered"

[[classification]]
type_name = "ExpiryEpochAdvanceSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:expiry-epoch-advance-spec"
source_location = "a12:266"
statement = "The sequenced command stream observes expiry only as one registered ExpiryEpochAdvanceSpec control command per configured epoch tick — the sole sequencer-visible expiry artifact, an ordered transition carrying subject-bound rollback-resistant TimeValidationEvidence; included per ruling C4, with L268 deferring exactly this row decision to G0."
status = "registered"

# --- F9 policy-revocation-timeauthority-privacy-dp (frozen ordinals 44-54).
# --- ELEVEN members: seven of them are the time-authority controls (46-52).
# --- DpTransitionSpec is ARMED (the eight x2ar-4 arms), so its row binds the
# --- member-root id (round-12 T4). Security binding contributes ZERO rows
# --- per I-9. Policy/revocation/privacy/DP lines live in Warden §12 prose,
# --- anchored under the topical slice a21 (the a12:256 precedent for
# --- out-of-range lines); the time-authority lines are in a16's range.

[[classification]]
type_name = "PolicyTransitionSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:policy-transition-spec"
source_location = "a21:878"
statement = "PolicyTransitionSpec carries the exact before-root binding, mutation body ref, candidate after root, and before-binding admin authorization; the candidate cannot authorize itself and must equal exact deterministic application with checked epoch increment."
status = "registered"

[[classification]]
type_name = "RevocationTransitionSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:revocation-transition-spec"
source_location = "a21:880"
statement = "RevocationTransitionSpec has the same before-root, candidate-after-root, before-root admin authorization, idempotency, and audit fields as the policy transition; it advances RevocationIndex once per nonempty command and can never remove/weaken an entry."
status = "registered"

[[classification]]
type_name = "TimeIssuanceAdmissionFreezeSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:time-issuance-admission-freeze-spec"
source_location = "a16:2183"
statement = "Each participating group first orders TimeIssuanceAdmissionFreezeSpec; its applied record blocks new reservations locally and exports GroupTimeIssuanceAdmissionFreezeCertificate — the first ordered step of the externally recoverable issuance-closing protocol."
status = "registered"

[[classification]]
type_name = "TimeAuthorityRotationIntentSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:time-authority-rotation-intent-spec"
source_location = "a16:2183"
statement = "TimeAuthorityRotationIntentSpec<Role:AuthorityOwningRole> is then the ordered Semantic authorization; apply atomically selects ClosingRequested and creates immutable TimeAuthorityRotationIntentRecord owning the exact sets, operation IDs, resource holds, authorization, and audit lineage."
status = "registered"

[[classification]]
type_name = "TimeAuthorityIssuanceCloseSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:time-authority-issuance-close-spec"
source_location = "a16:2185"
statement = "TimeAuthorityIssuanceCloseSpec<Role:AuthorityOwningRole> consumes the intent, receipt, dispatch terminal evidence, exact group-freeze set, and current registry and changes only ClosingRequested -> IssuanceClosing."
status = "registered"

[[classification]]
type_name = "TimeAuthorityIssuanceFenceAuthorizeSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:time-authority-issuance-fence-authorize-spec"
source_location = "a16:2187"
statement = "TimeAuthorityIssuanceFenceAuthorizeSpec is an allowed successor spec tag of the non-widenable TimeAuthorityTransitionCompletionAuthority, constructible only from the rooted intent plus the exact dispatch attempt or terminal evidence at or beyond the listed irreversible state."
status = "registered"

[[classification]]
type_name = "TimeAuthorityRegistryTransitionAuthorizeSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:time-authority-registry-transition-authorize-spec"
source_location = "a16:2187"
statement = "TimeAuthorityRegistryTransitionAuthorizeSpec is an allowed successor spec tag of the same non-widenable completion authority; the external registry-head CAS it authorizes rides the portable pre-CAS TimeAuthorityEpochTransitionPlan."
status = "registered"

[[classification]]
type_name = "TimeAuthorityProfileTransitionSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:time-authority-profile-transition-spec"
source_location = "a16:2221"
statement = "Exact activation is TimeAuthorityProfileTransitionSpec<Role:AuthorityOwningRole>: it verifies the complete intent/dispatch/closing/quiescence/fence/inventory bijection and atomically selects the successor for new issuance while retaining the predecessor as Draining."
status = "registered"

[[classification]]
type_name = "TimeAuthorityProfileRetirementSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:time-authority-profile-retirement-spec"
source_location = "a16:2223"
statement = "Exact retirement is TimeAuthorityProfileRetirementSpec<Role:AuthorityOwningRole>: apply first verifies the heavy closure, deterministically creates TimeAuthorityTransitionTerminalSummary, selects Retired{terminal_summary_ref}, and releases the drain hold only after the paired floors are durable."
status = "registered"

[[classification]]
type_name = "PrivacyContinuityImportSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:privacy-continuity-import-spec"
source_location = "a21:934"
statement = "The sole Chronicle import is bounded PrivacyContinuityImportSpec<Role> for Local or Meta with exact expected current portable head, local ledger/request-index, current profile refs, and a nonempty direct-successor chain; the required Local semantic row is spelled at L1918. There is no RecordCharge command."
status = "registered"

[[classification]]
type_name = "DpTransitionSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:dp-transition-spec"
source_location = "a21:936"
statement = "DpTransitionSpec<Role> has exactly Prepare, Abandon, ArmCharge, Start, Yield, Reclaim, CommitResult, and Compact; its u8 arm tags follow that source order. ChargeRefused, attach-to-ChargedIdle, ChargedMaterialPending, and UnavailableAfterCharge are selected only by PrivacyContinuityImportSpec charge bindings and exact restore/import dispositions, never by a DpTransitionSpec arm."
status = "registered"

# --- F10 audit (frozen ordinals 55-60). SIX members; AuditTerminalFreezeSpec
# --- is ARMED (Arm|BeginRelease|FinalizeRelease, L1834), so its row binds the
# --- member-root id (round-12 T4). AuditSigningAttemptSupersedeSpec stays
# --- MAINTENANCE per I-4 (L1848 "nonsemantic maintenance input") and
# --- AuditVisibilityAdvanceSpec is Protocol-plane maintenance — neither takes
# --- a Semantic classification row here; both remain Phase B maintenance
# --- rows. Warden §12 audit lines (948-986) anchor under the topical slice
# --- a21; the freeze/abandon defining lines (1834/1840) are in a08's range.

[[classification]]
type_name = "AuditTicketAdmissionSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:audit-ticket-admission-spec"
source_location = "a21:2713"
statement = "Exact AuditTicketAdmissionSpec is the 5-member CAS-import record; its validator requires the direct TicketIssued successor, exact sequence increment, exact ticket/unresolved addition, matching receipt/CAS versions, and no unrelated change; Semantic per L982 with a StructurallyInapplicable{AuditSystemAuthority} gate."
status = "registered"

[[classification]]
type_name = "AuditTerminalFreezeSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:audit-terminal-freeze-spec"
source_location = "a08:1834"
statement = "AuditTerminalFreezeSpec is the closed lifecycle-scaffolding transition Arm|BeginRelease|FinalizeRelease; Arm atomically validates the complete basis, installs its hold, and freezes it before any terminal signature; BeginRelease selects ReleasePending without dropping the hold; FinalizeRelease alone produces Released."
status = "registered"

[[classification]]
type_name = "AuditTerminalPlanAbandonSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:audit-terminal-plan-abandon-spec"
source_location = "a08:1840"
statement = "AuditTerminalPlanAbandonSpec is the exact freeze-backed Local-or-Meta Semantic input, legal only while the freeze is ReleasePending; apply creates AuditTerminalPlanAbandonment and marks the semantic plan AbandonedForFreezeRelease; SEMANTIC per ruling I-4, with the required Local row spelled at L1918."
status = "registered"

[[classification]]
type_name = "AuditTerminalSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:audit-terminal-spec"
source_location = "a21:2713"
statement = "Exact AuditTerminalSpec is the 4-member body consuming exactly the already-pending ticket; it is the non-mutating/pre-effect terminal path, not a second mutation terminal, may not request another ticket, and rejects every scope/outcome mismatch."
status = "registered"

[[classification]]
type_name = "AuditRecoverySpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:audit-recovery-spec"
source_location = "a21:982"
statement = "Narrowly scoped AuditRecoverySpec is one of the audit lifecycle semantic transitions that cannot recurse through their own gate, carrying TerminalAuditGate::StructurallyInapplicable{AuditSystemAuthority} in its sequence-neutral wrapper; orphan-ticket recovery uses the same admission relation (L948)."
status = "registered"

[[classification]]
type_name = "AuditCompletenessTransitionSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:audit-completeness-transition-spec"
source_location = "a21:986"
statement = "AuditCompletenessTransitionSpec<Role> imports the exact CompletenessChanged successor/receipt pair locally through the same pipeline rules with terminal_audit_gate:StructurallyInapplicable{AuditSystemAuthority}; the continuity status and future profile validator permanently exclude the recorded gap."
status = "registered"

# --- F11 bulk-load-staging (frozen ordinal 61). ONE ARMED member; its row
# --- binds the member-root id (round-12 T4). Committed is intent-carried per
# --- L637/L639 and the x2ar-3 ruling — no arm. Anchored at the in-range
# --- family-sentence line a10:1918 (the L637 defining line sits in §7,
# --- outside every slice range, with no topical audit/bulk slice owning it;
# --- a10:1918 names the required row directly and is the established anchor
# --- for family-sentence-forced classes).

[[classification]]
type_name = "BulkLoadTransitionSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:bulk-load-transition-spec"
source_location = "a10:1918"
statement = "Required Local semantic rows include BulkLoadTransitionSpec<Local>; per L637 it has exactly Reserve, AppendChunk, Seal, PrepareCommit, and Abort — its u8 arm tags follow that source order, each is an explicitly registered Local/Meta staging control, and Committed is reached only by the transaction-carried BulkLoad intent, never by a staging-control arm."
status = "registered"

# --- F12 derived-build (frozen ordinal 62). ONE ARMED member; its row binds
# --- the member-root id (round-12 T4). Schema activation contributes ZERO
# --- rows per I-9 — Active is selected only by the committed schema-
# --- activation path (L643). Anchored at the in-range family-sentence line
# --- a10:1918 (L643 sits in §7 outside every slice range; the same
# --- treatment as BulkLoadTransitionSpec above).

[[classification]]
type_name = "DerivedBuildTransitionSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:derived-build-transition-spec"
source_location = "a10:1918"
statement = "Required Local semantic rows include DerivedBuildTransitionSpec<Local>; per L643 it has exactly Reserve, PublishSnapshotProgress, PublishCatchupProgress, BeginValidation, PublishReady, and Abort — every progress update is monotone/idempotent, owns its bounded closure, and advertises its exact watermark; the validation certificate names no future activation."
status = "registered"

# --- F13 keys (frozen ordinals 63-65). KeyDestroyProposal remains a
# --- PreOrderArtifact from the creation population; only the three exact
# --- ordered Local semantic transitions gain RegisteredCommandInput rows.

[[classification]]
type_name = "KeyDestroyAuthorizeSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:key-destroy-authorize-spec"
source_location = "a15:2059"
statement = "KeyDestroyAuthorizeSpec is the ordered Local authorization carrying KeyDestroyProposal; apply atomically installs the no-new-reference quarantine plus one immutable authorization recipe per and only per target plan, then moves the key to DestroyPending before physical dispatch."
status = "registered"

[[classification]]
type_name = "KeyDestroyFinalizeSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:key-destroy-finalize-spec"
source_location = "a15:2067"
statement = "KeyDestroyFinalizeSpec consumes the exact quarantine publication, proposal/recipe/Protocol-terminal/typed-receipt bijection and current zero-reference proof; apply selects DestroyedPendingCertificate and creates compact KeyDestroyRecord."
status = "registered"

[[classification]]
type_name = "KeyDestroyCertificatePublishSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:key-destroy-certificate-publish-spec"
source_location = "a15:2069"
statement = "KeyDestroyCertificatePublishSpec is the sole ordered transition from DestroyedPendingCertificate to final Destroyed and installs the compact certificate-index edge only after the matching record, durable root, audit visibility advance, and quorum certificate exist."
status = "registered"

# --- F15A semantic backup (frozen ordinals 70-79). These ten explicit Local
# --- members are classified in their confirmed source order. The structural
# --- Role cohort and the restore/DirectoryBound members remain later F15
# --- increments; Protocol dispatch inputs are not laundered into this table.

[[classification]]
type_name = "LocalBackupBarrierSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-backup-barrier-spec"
source_location = "a15:2117"
statement = "LocalBackupBarrierSpec is the ordered Local backup barrier; exact current Local roots, coordinates, checkpoint/configuration/floor and generated closure are validated before apply creates the complete LocalBackupPinRecord source-retention lease."
status = "registered"

[[classification]]
type_name = "LocalBackupClosurePublishSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-backup-closure-publish-spec"
source_location = "a15:2121"
statement = "LocalBackupClosurePublishSpec is the sole Pinned-to-ClosureCertified transition and byte-matches the pin, copy verification and Local closure certificate without dropping the source pin."
status = "registered"

[[classification]]
type_name = "LocalBackupSealSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-backup-seal-spec"
source_location = "a15:2121"
statement = "LocalBackupSealSpec requires the exact pin/copy/certificate/manifest closure and alone selects Sealed; it cannot skip closure publication or perform external publication."
status = "registered"

[[classification]]
type_name = "LocalBackupPublicationAuthorizeSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-backup-publication-authorize-spec"
source_location = "a15:2121"
statement = "LocalBackupPublicationAuthorizeSpec is the sole transition out of Sealed and creates the exact immutable publication operation record as AuthorizedUndispatchedRecipe without I/O."
status = "registered"

[[classification]]
type_name = "LocalBackupPublicationReceiptImportSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-backup-publication-receipt-import-spec"
source_location = "a15:2121"
statement = "LocalBackupPublicationReceiptImportSpec consumes the portable dispatch terminal evidence and exact publication receipt and selects only the policy-matched pending transparency or pending grant state."
status = "registered"

[[classification]]
type_name = "LocalBackupGrantIssueImportSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-backup-grant-issue-import-spec"
source_location = "a15:2123"
statement = "LocalBackupGrantIssueImportSpec byte-matches publication, optional required transparency, grant dispatch, Active grant and current authority observation; alone constructs the Local projection and installs Published."
status = "registered"

[[classification]]
type_name = "LocalBackupArtifactVerifySpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-backup-artifact-verify-spec"
source_location = "a15:2123"
statement = "LocalBackupArtifactVerifySpec consumes the exact current-use guard and post-publication artifact proof and installs ArtifactVerified without releasing the source pin."
status = "registered"

[[classification]]
type_name = "LocalBackupReleaseSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-backup-release-spec"
source_location = "a15:2123"
statement = "LocalBackupReleaseSpec is the sole Local pin-removing transition after exact publication, grant, artifact and Activated-hold lineage; it enters ReleasedPendingArchiveHoldCompletion, never final Released."
status = "registered"

[[classification]]
type_name = "ArchiveSourceReleaseCompletionImportSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:archive-source-release-completion-import-spec"
source_location = "a15:2107"
statement = "ArchiveSourceReleaseCompletionImportSpec<Role> is the only ordered transition from ReleasedPendingArchiveHoldCompletion to compact Released after byte-matching the completed external CAS lineage; this row binds the Local instantiation."
status = "registered"

[[classification]]
type_name = "LocalBackupAbortSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-backup-abort-spec"
source_location = "a15:2139"
statement = "LocalBackupAbortSpec is the only Local backup failure transition and is legal only with complete provider-linearizable no-publication evidence, exact cleanup completion and no-other-owner proof; unresolved SentUnknown forces roll-forward."
status = "registered"

# --- F15B semantic restore (frozen ordinals 80-86). RestoreAbandonSpec is
# --- an armed family and therefore binds the member-root contract id; the
# --- Local role-valid arm is the sole contract row in this increment.

[[classification]]
type_name = "LocalRestoreActivationSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-restore-activation-spec"
source_location = "a20:2603"
statement = "LocalRestoreActivationSpec consumes the exact Local ready certificate and hidden-state/configuration/checkpoint/floor/promotion-authority basis; it alone moves Reconciling to VisibilityCommitted and permits the gap-free activation certificate without exposing service."
status = "registered"

[[classification]]
type_name = "LocalRestoreServicePrepareSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-restore-service-prepare-spec"
source_location = "a20:2603"
statement = "LocalRestoreServicePrepareSpec consumes the activation certificate and one closed promotion-authority profile recipe; it alone selects ServicePreparedHidden and permits the gap-free prepare certificate."
status = "registered"

[[classification]]
type_name = "LocalRestoreServicePromotionSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-restore-service-promotion-spec"
source_location = "a20:2605"
statement = "LocalRestoreServicePromotionSpec requires exact prepare, Local manifest, composite receipt, profile-stage lineage and hidden-state equality; after durable semantic visibility it enters PromotedAwaitingReopen and retains the source lease."
status = "registered"

[[classification]]
type_name = "LocalRestoreServiceCompletionSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-restore-service-completion-spec"
source_location = "a20:2605"
statement = "LocalRestoreServiceCompletionSpec consumes the exact promotion lineage, installed seal and fresh independent reopen proof; it alone selects Operational and AwaitingSourceAccessCleanup without dropping the source lease or terminal pin."
status = "registered"

[[classification]]
type_name = "LocalRestoreAbandonFinalizeSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-restore-abandon-finalize-spec"
source_location = "a18:2453"
statement = "For ExternalCas, LocalRestoreAbandonFinalizeSpec consumes exact canonical profile terminal evidence, clears restore_state_ref, selects RecoveryAbandoned, materializes the Local no-resurrection tombstone and enters AbandonedAwaitingTerminalPin."
status = "registered"

[[classification]]
type_name = "LocalRestoreAbandonmentPinInstallSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:local-restore-abandonment-pin-install-spec"
source_location = "a18:2381"
statement = "LocalRestoreAbandonmentPinInstallSpec consumes durable abandonment publication evidence, the published tombstone, exact abandoned physical inventory, pin durability receipt and no-other-owner proof; it installs the abandoned terminal pin basis and cleanup progress generation zero."
status = "registered"

[[classification]]
type_name = "RestoreAbandonSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:restore-abandon-spec"
source_location = "a18:2433"
statement = "RestoreAbandonSpec is the closed Local|ShardedMeta ordered union; the Local arm requires a hidden target, exact no-target-observation proof, closed Local authority profile, prebuilt pin pending owner and ReleaseRequired source lease, then remains hidden in AbandonAuthorizedHidden."
status = "registered"

# --- F15C DirectoryBound restore authority (frozen ordinals 87-90). All four
# --- are armless ordered Local Semantic inputs. They reserve the inode-fenced
# --- pending/final-authority and two-step abandonment transitions without
# --- claiming an external-continuity action or a live runtime handler.

[[classification]]
type_name = "DirectoryBoundEnterPromotionPendingSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:directory-bound-enter-promotion-pending-spec"
source_location = "a20:2588"
statement = "DirectoryBoundEnterPromotionPendingSpec consumes the exact creation evidence, hidden prepare certificate, profile-matched reservation or zero-catalog basis and live inode publication authority; only its durable RootSlot successor permits the pending receipt."
status = "registered"

[[classification]]
type_name = "DirectoryBoundFinalizeOperationalAuthoritySpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:directory-bound-finalize-operational-authority-spec"
source_location = "a20:2588"
statement = "DirectoryBoundFinalizeOperationalAuthoritySpec consumes the profile-matched activation or embedded admission lineage and exact pending state; only its durable RootSlot successor permits the distinct operational-authority receipt."
status = "registered"

[[classification]]
type_name = "DirectoryBoundAbandonApplySpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:directory-bound-abandon-apply-spec"
source_location = "a18:2446"
statement = "DirectoryBoundAbandonApplySpec is the inode-fenced Local abandonment apply; it enters AbandonedAuthorityAppliedAwaitingReceipt with only the assigned apply identity, skeleton, pending owner and recipes, never its future receipt or tombstone."
status = "registered"

[[classification]]
type_name = "DirectoryBoundAbandonReceiptImportSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:directory-bound-abandon-receipt-import-spec"
source_location = "a18:2446"
statement = "DirectoryBoundAbandonReceiptImportSpec consumes the durable DirectoryBound receipt plus exact awaiting-root publication evidence; it alone materializes the Local authority-owning tombstone and enters AbandonedAwaitingTerminalPin."
status = "registered"

# --- F15D first authority-owning restore/lease cleanup cohort (frozen
# --- ordinals 91-93). The generic plan members instantiate structurally for
# --- Local under the owner-confirmed C5 ruling; all remain reserved commands.

[[classification]]
type_name = "RestoreSourceKeyAccessCleanupFinalizeSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:restore-source-key-access-cleanup-finalize-spec"
source_location = "a18:2351"
statement = "RestoreSourceKeyAccessCleanupFinalizeSpec<Role> proves no unresolved renewal, constructs the cleanup accumulator, record, release body and renewal-close record, and atomically enters AwaitingSourceRelease plus RenewalClosed."
status = "registered"

[[classification]]
type_name = "RestoreSourceLeaseRenewAuthorizedNeverArmedFinalizeSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:restore-source-lease-renew-authorized-never-armed-finalize-spec"
source_location = "a18:2353"
statement = "RestoreSourceLeaseRenewAuthorizedNeverArmedFinalizeSpec<Role> consumes the renew operation record, zero-attempt proof, ineligibility evidence and nonreuse tombstone, then appends AuthorizedNeverArmed and returns the unchanged lease to Current."
status = "registered"

[[classification]]
type_name = "RestoreSourceLeaseReleaseAuthorizedNeverArmedFinalizeSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:restore-source-lease-release-authorized-never-armed-finalize-spec"
source_location = "a18:2353"
statement = "RestoreSourceLeaseReleaseAuthorizedNeverArmedFinalizeSpec<Role> is the symmetric pre-Arm release finalizer for administrative cancellation or invalidated authority; lease expiry alone does not force it."
status = "registered"

# --- F15E terminal-pin release and source-access cleanup cohort (frozen
# --- ordinals 94-96). The generic plan members instantiate structurally for
# --- Local under the owner-confirmed C5 ruling; all remain reserved commands.

[[classification]]
type_name = "RestoreTerminalPinReleaseFinalizeSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:restore-terminal-pin-release-finalize-spec"
source_location = "a18:2371"
statement = "RestoreTerminalPinReleaseFinalizeSpec consumes a completed per-member pin release, the matching Active or ReleaseAuthorized entry and a current no-other-owner proof; only durable apply selects Released and permits the registry to drop the basis."
status = "registered"

[[classification]]
type_name = "RestoreSourceKeyAccessCleanupAuthorizeSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:restore-source-key-access-cleanup-authorize-spec"
source_location = "a18:2381"
statement = "RestoreSourceKeyAccessCleanupAuthorizeSpec<Role> uses the universal dispatch authority to freeze one exact explicit source-resource revocation operation and advance only that cleanup-progress member to Authorized."
status = "registered"

[[classification]]
type_name = "RestoreSourceKeyAccessCleanupImportSpec"
class = "RegisteredCommandInput"
command_contract_id = "cc:local:restore-source-key-access-cleanup-import-spec"
source_location = "a18:2381"
statement = "RestoreSourceKeyAccessCleanupImportSpec<Role> imports and byte-matches one authorized source-resource revocation's terminal lineage before advancing that cleanup-progress member to its typed terminal disposition."
status = "registered"
