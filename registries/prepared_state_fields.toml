# Canonical PreparedOwnership-plane field inventory. Registry-check holds this
# projection in exact bijection with durable_state_slots.toml; the first Meta
# row is the current-root basis consumed by NeverRegisteredFloorSpec.
schema_version = 1
registry_epoch = 2
plane = "PreparedOwnership"

[[field]]
role = "Meta"
slot_tag = "meta_prepared_payload_root"
status = "reserved"
